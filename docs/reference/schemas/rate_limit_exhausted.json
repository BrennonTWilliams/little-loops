{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "little-loops://event-rate_limit_exhausted.json",
  "title": "Rate Limit Exhausted",
  "description": "Emitted when the wall-clock rate-limit budget is spent across short + long tiers.",
  "type": "object",
  "required": [
    "event",
    "ts",
    "state",
    "retries"
  ],
  "properties": {
    "event": {
      "type": "string",
      "description": "Event type identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp"
    },
    "run_id": {
      "type": "string",
      "description": "Run-scoped identity, stable across a run (including pause/resume). Not required: only FSMExecutor._emit()-routed events stamp it today (ENH-3345); parallel.*/issue.* emitters don't yet (ENH-3346)."
    },
    "loop": {
      "type": "string",
      "description": "Loop name, stamped on every FSMExecutor._emit()-routed event (ENH-3345). Not required for the same reason as run_id."
    },
    "state": {
      "type": "string",
      "description": "State name that exhausted rate-limit retries"
    },
    "retries": {
      "type": "integer",
      "description": "Total rate-limit retries attempted (short + long)"
    },
    "short_retries": {
      "type": "integer",
      "description": "Retries attempted in the short-burst tier"
    },
    "long_retries": {
      "type": "integer",
      "description": "Retries attempted in the long-wait tier"
    },
    "total_wait_seconds": {
      "type": "number",
      "description": "Accumulated wall-clock seconds spent in rate-limit waits"
    },
    "next": {
      "type": [
        "string",
        "null"
      ],
      "description": "Next state the FSM transitions to, or null if none"
    }
  },
  "additionalProperties": true
}
