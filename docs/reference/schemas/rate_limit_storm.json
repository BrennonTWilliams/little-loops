{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "little-loops://event-rate_limit_storm.json",
  "title": "Rate Limit Storm",
  "description": "Emitted when consecutive rate_limit_exhausted events reach the storm threshold.",
  "type": "object",
  "required": [
    "event",
    "ts",
    "state",
    "count"
  ],
  "properties": {
    "event": {
      "type": "string",
      "description": "Event type identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp"
    },
    "state": {
      "type": "string",
      "description": "State name that triggered the storm threshold"
    },
    "count": {
      "type": "integer",
      "description": "Consecutive rate_limit_exhausted count at emission time"
    }
  },
  "additionalProperties": true
}
