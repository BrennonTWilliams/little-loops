{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "little-loops://event-parallel.worker_started.json",
  "title": "Parallel: Worker Started",
  "description": "Emitted by WorkerPool._process_issue immediately after worktree creation, when a worker begins processing an issue (ENH-3346).",
  "type": "object",
  "required": [
    "event",
    "ts",
    "worker_id",
    "issue_id",
    "worktree_path",
    "branch",
    "run_id"
  ],
  "properties": {
    "event": {
      "type": "string",
      "description": "Event type identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp"
    },
    "run_id": {
      "type": "string",
      "description": "Run-scoped identity, stable across a run (including pause/resume). Stamped by FSMExecutor._emit()-routed events (ENH-3345) and by every parallel.* emitter (ENH-3346); required on all parallel.* events, optional elsewhere."
    },
    "loop": {
      "type": "string",
      "description": "Loop name, stamped on every FSMExecutor._emit()-routed event (ENH-3345). Not required for the same reason as run_id."
    },
    "worker_id": {
      "type": "string",
      "description": "Worker identifier, aliased to issue_id (stable for the worker's lifetime)"
    },
    "issue_id": {
      "type": "string",
      "description": "Issue identifier this worker is processing"
    },
    "worktree_path": {
      "type": "string",
      "description": "Filesystem path to the worker's git worktree"
    },
    "branch": {
      "type": "string",
      "description": "Git branch created for this worker"
    }
  },
  "additionalProperties": true
}
