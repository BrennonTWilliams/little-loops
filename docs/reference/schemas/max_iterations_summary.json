{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "little-loops://event-max_iterations_summary.json",
  "title": "Max Iterations Summary",
  "description": "Emitted when the iteration cap fires and on_max_iterations is set; signals that a summary state will run before the loop terminates.",
  "type": "object",
  "required": [
    "event",
    "ts",
    "summary_state",
    "iterations"
  ],
  "properties": {
    "event": {
      "type": "string",
      "description": "Event type identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp"
    },
    "summary_state": {
      "type": "string",
      "description": "Name of the summary state the executor transitions to"
    },
    "iterations": {
      "type": "integer",
      "description": "Iteration count at which the cap fired"
    }
  },
  "additionalProperties": true
}
