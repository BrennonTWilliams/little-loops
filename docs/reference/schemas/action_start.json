{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "little-loops://event-action_start.json",
  "title": "Action Start",
  "description": "Emitted when a state action begins.",
  "type": "object",
  "required": [
    "event",
    "ts",
    "action",
    "is_prompt"
  ],
  "properties": {
    "event": {
      "type": "string",
      "description": "Event type identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp"
    },
    "run_id": {
      "type": "string",
      "description": "Run-scoped identity, stable across a run (including pause/resume). Not required: only FSMExecutor._emit()-routed events stamp it today (ENH-3345); parallel.*/issue.* emitters don't yet (ENH-3346)."
    },
    "loop": {
      "type": "string",
      "description": "Loop name, stamped on every FSMExecutor._emit()-routed event (ENH-3345). Not required for the same reason as run_id."
    },
    "action": {
      "type": "string",
      "description": "Action name or command"
    },
    "is_prompt": {
      "type": "boolean",
      "description": "True if action is a Claude prompt, false for shell command"
    }
  },
  "additionalProperties": true
}
