name: html-website-generator
category: harness
input_key: description
required_inputs: ["description"]
# index.html is the single deliverable; latest-wins overwrite is intentional.
artifact_versioning_ok: true
description: |
  Generator-evaluator harness for single-page HTML website creation.
  Accepts a natural language website description via loop_input and
  iteratively generates and refines HTML/CSS/JS using Playwright CLI
  screenshot evaluation. Implements the GAN-style architecture from
  Anthropic's harness design article. Thin wrapper over
  oracles/generator-evaluator (ENH-1869).
initial: plan
max_iterations: 30
timeout: 14400

# HARNESS: Context variables control quality threshold.
context:
  description: ""             # populated from loop_input via input_key: description
  pass_threshold: 6           # minimum score per criterion (1-10) to pass
  design_tokens_context: ""   # runner-injected; resolved semantic token values or empty

states:

  plan:
    # HARNESS: Planner phase — expands the one-line description into a design brief.
    # This mirrors the "generator receives a spec" role from the harness design article.
    # The brief captures visual identity, layout, and unique creative angle so the
    # generator has strong direction on the first iteration.
    action_type: prompt
    action: |
      Create directory ${context.run_dir} if it does not already exist.

      Write a design brief to ${context.run_dir}/brief.md for the following website:
      "${context.description}"

      The brief must include:
      - **Visual identity**: specific color palette (hex values), typeface choices, overall mood and tone
      - **Layout structure**: major sections, key components, hierarchy
      - **Unique creative angle**: what makes this design distinctive and memorable? Avoid clichés.
      - **Anti-patterns to avoid**: purple gradients on white cards, unmodified stock components,
        generic hero sections, AI-generated filler patterns

      Be specific and opinionated. The generator will use this brief as its primary reference.
    next: run_gen_eval

  run_gen_eval:
    # HARNESS: Thin-wrapper delegation — runs generate → evaluate (Playwright screenshot) →
    # score (LLM rubric) inside oracles/generator-evaluator until ALL_PASS or max_iterations.
    # Routes to smoke_test on success; failed on error or exhausted retries (ENH-1869).
    loop: oracles/generator-evaluator
    with:
      run_dir: "${context.run_dir}"
      generate_prompt: |
        Read ${context.run_dir}/brief.md for the design brief.

        A previous iteration may have written ${context.run_dir}/critique.md. If
        that file exists, read it and address every issue in its "Issues to
        Address" section specifically and completely. If it does not exist,
        proceed silently — do not narrate the check.

        ${context.design_tokens_context}

        Write a single self-contained HTML file to ${context.run_dir}/index.html that:
        - Implements the visual identity and layout from the brief
        - Embeds all CSS and JavaScript inline (no external dependencies, no CDN links)
        - Produces a responsive layout that works at desktop and mobile widths
        - Achieves a distinctive, museum-quality aesthetic — avoid AI-slop patterns
          (generic gradients, unmodified Bootstrap components, stock imagery placeholders,
          bland sans-serif on white backgrounds)
        - If design tokens are provided above, use their semantic names and resolved values
          for colors; do not invent new color values outside the token palette
        - If critique exists, resolves every specific issue before introducing new ideas
      rubric: |
        Read the screenshot at ${context.run_dir}/screenshot.png to view the generated website.
        Also read ${context.run_dir}/brief.md to recall the design intent.

        Score the website on each criterion from 1 to 10 and write the evaluation to
        ${context.run_dir}/critique.md using this exact format:

        ```
        # Evaluation

        design_quality: N/10 — [explanation: does the design feel like a coherent whole?
          Strong color/typography/layout combination creates a distinct mood and identity.]
        originality: N/10 — [explanation: evidence of custom creative decisions?
          Best designs are museum-quality. Penalize: purple gradients on white cards,
          unmodified stock components, AI-generated fill patterns.]
        craft: N/10 — [explanation: technical execution — typography hierarchy, spacing
          consistency, color harmony, contrast ratios, pixel-level attention.]
        functionality: N/10 — [explanation: can users understand what the site does and
          complete the primary task within 5 seconds?]

        ## Issues to Address
        [Specific, actionable critique for the generator. List each problem concisely
         so the generator can address it directly on the next pass. If all scores meet
         threshold, write "No issues — all criteria pass."]
        ```

        After writing the file, evaluate the scores:
        - design_quality weight: 2x (threshold 6)
        - originality weight: 2x (threshold 6)
        - craft weight: 1x (threshold 6)
        - functionality weight: 1x (threshold 6)

        If ALL four individual scores are >= 6, output exactly: ALL_PASS
        Otherwise output: ITERATE
      pass_threshold: 6
    on_yes: smoke_test
    on_no: failed
    on_error: failed

  smoke_test:
    # HARNESS: Shell-level functional verification after LLM scoring.
    # Runs Playwright to check for JS console errors and meaningful text
    # content before accepting the artifact. Decoupling routing from the
    # LLM prompt prevents self-certification inflation — an LLM may claim
    # ALL_PASS without actually processing the screenshot when multimodal
    # input is unavailable.
    action_type: shell
    action: |
      NODE_PATH="$(npm root -g)" node -e "
      const { chromium } = require('@playwright/test');
      (async () => {
        const browser = await chromium.launch();
        const page = await browser.newPage();
        const errors = [];
        page.on('pageerror', e => errors.push(e.message));
        await page.goto('file://' + process.cwd() + '/${context.run_dir}/index.html');
        await page.waitForTimeout(1500);
        const textLen = await page.evaluate(() => document.body.innerText.trim().length);
        if (textLen < 20) { console.log('FAIL:minimal_content'); process.exit(1); }
        if (errors.length > 0) { console.log('FAIL:js_errors:', errors.join('; ')); process.exit(1); }
        console.log('SMOKE_PASS');
        await browser.close();
      })().catch(e => { console.error('SMOKE_FAIL:', e.message); process.exit(1); });
      "
    evaluate:
      type: output_contains
      pattern: "SMOKE_PASS"
    on_yes: vision_gate
    on_no: run_gen_eval
    on_error: run_gen_eval

  vision_gate:
    # HARNESS: Optional external-vision evaluator (ENH: VISION_* env wiring).
    # Decouples *aesthetic* scoring from the host LLM's self-grade — the same
    # anti-self-certification motive behind smoke_test, but for visual quality
    # rather than functional sanity. An independent vision model scores the
    # screenshot against the rubric; criteria below threshold are appended to
    # critique.md and the loop routes back to run_gen_eval to refine + re-score.
    #
    # GRACEFUL DEGRADATION: this state is a no-op pass unless VISION_BASE_URL,
    # VISION_MODEL and VISION_API_KEY are all set. The runner does NOT auto-load
    # .env and ${env.X} hard-fails when unset, so the check is done in-shell by
    # sourcing .env. API/parse/network errors also pass (never block shipping a
    # functionally-sound artifact on an optional gate). A per-run round cap
    # (.vision_rounds in run_dir) bounds refine/re-score ping-pong.
    action_type: shell
    action: |
      RUN_DIR="${context.run_dir}"
      case "$$RUN_DIR" in
        /*) ABS_DIR="$$RUN_DIR" ;;
        *) ABS_DIR="$$(pwd)/$$RUN_DIR" ;;
      esac
      if [ -f .env ]; then set -a; . ./.env; set +a; fi
      if [ -z "$${VISION_BASE_URL:-}" ] || [ -z "$${VISION_MODEL:-}" ] || [ -z "$${VISION_API_KEY:-}" ]; then
        echo "VISION_PASS: skipped (VISION_* env not configured)"; exit 0
      fi
      if [ ! -f "$$ABS_DIR/screenshot.png" ]; then
        echo "VISION_PASS: skipped (no screenshot.png)"; exit 0
      fi
      export ABS_DIR
      python3 <<'PY'
      import os, re, sys, json, base64, urllib.request
      abs_dir = os.environ["ABS_DIR"]
      shot = os.path.join(abs_dir, "screenshot.png")
      rc_file = os.path.join(abs_dir, ".vision_rounds")
      crit = os.path.join(abs_dir, "critique.md")
      ROUND_CAP = 3
      try:
          rc = int(open(rc_file).read().strip())
      except Exception:
          rc = 0
      if rc >= ROUND_CAP:
          print("VISION_PASS: round cap (%d) reached, accepting" % ROUND_CAP); sys.exit(0)
      try:
          with open(shot, "rb") as f:
              b64 = base64.b64encode(f.read()).decode()
      except Exception as e:
          print("VISION_PASS: screenshot unreadable (%s)" % e); sys.exit(0)
      base = os.environ["VISION_BASE_URL"].rstrip("/")
      endpoint = base if base.endswith("completions") else base + "/chat/completions"
      rubric = (
          "You are a strict, impartial design critic. Study the attached "
          "screenshot of a generated website and score it 1-10 on each:\n"
          "- design_quality: does it read as a coherent visual whole?\n"
          "- originality: custom, museum-quality decisions. Penalize purple "
          "gradients on white cards, unmodified stock components, AI-slop fill.\n"
          "- craft: typography hierarchy, spacing consistency, color harmony, contrast.\n"
          "- functionality: is the site's primary purpose clear within 5 seconds?\n"
          "Threshold: every criterion must be >= 6.\n"
          'Reply with ONE JSON object only, no prose: {"scores": '
          '{"design_quality": N, "originality": N, "craft": N, '
          '"functionality": N}, "issues": ["specific actionable fix", ...]}'
      )
      payload = {
          "model": os.environ["VISION_MODEL"],
          "temperature": 0.2,
          "max_tokens": 900,
          "messages": [{"role": "user", "content": [
              {"type": "text", "text": rubric},
              {"type": "image_url", "image_url": {"url": "data:image/png;base64," + b64}},
          ]}],
      }
      req = urllib.request.Request(
          endpoint, data=json.dumps(payload).encode(),
          headers={"Authorization": "Bearer " + os.environ["VISION_API_KEY"],
                   "Content-Type": "application/json"})
      try:
          with urllib.request.urlopen(req, timeout=120) as r:
              content = json.load(r)["choices"][0]["message"]["content"]
      except Exception as e:
          print("VISION_PASS: vision API error (%s), not blocking" % e); sys.exit(0)
      m = re.search(r"\{.*\}", content, re.S)
      try:
          data = json.loads(m.group(0)) if m else {}
          scores = data.get("scores", {}) or {}
          issues = data.get("issues", []) or []
      except Exception:
          print("VISION_PASS: unparseable vision verdict, not blocking"); sys.exit(0)
      failing = {k: v for k, v in scores.items()
                 if isinstance(v, (int, float)) and v < 6}
      print("VISION scores: " + json.dumps(scores))
      if not failing:
          print("VISION_PASS"); sys.exit(0)
      open(rc_file, "w").write(str(rc + 1))
      lines = ["", "## Issues to Address (external vision critique, round %d)" % (rc + 1)]
      for k, v in failing.items():
          lines.append("- %s scored %s/10 (below threshold 6)" % (k, v))
      for i in issues:
          lines.append("- %s" % i)
      with open(crit, "a") as f:
          f.write("\n".join(lines) + "\n")
      print("VISION_FAIL: %d criteria below threshold" % len(failing))
      PY
    evaluate:
      type: output_contains
      pattern: "VISION_PASS"
    on_yes: done
    on_no: run_gen_eval
    on_error: done

  done:
    # HARNESS: Terminal state — reached when all criteria pass or max_iterations exhausted.
    # The final website is at ${context.run_dir}/index.html.
    # The last critique (with final scores) is at ${context.run_dir}/critique.md.
    terminal: true

  failed:
    # HARNESS: Failure terminal — reached when the sub-loop or smoke_test errors out.
    # Signals an explicit failure so the outer loop / log reader can distinguish
    # "loop bailed cleanly" from "loop is still iterating". The last artifacts
    # (brief.md, any partial index.html, the last critique.md if one was written)
    # remain in ${context.run_dir} for inspection.
    terminal: true
