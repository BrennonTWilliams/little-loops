"""Spike: prove `git show <ref>:<path>` blob-at-ref read for FEAT-2652.

Standalone spike package. Not imported by production code; promoted to
`scripts/little_loops/spike/git_show_blob_at_ref/` only if adopted.
"""
