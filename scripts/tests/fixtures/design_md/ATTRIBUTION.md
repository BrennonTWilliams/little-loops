# Attribution

`paws_and_paths_DESIGN.md` in this directory is vendored, unmodified, from:

- **Upstream repository**: https://github.com/google-labs-code/design.md
- **File**: `examples/paws-and-paths/DESIGN.md`
- **License**: Apache-2.0 (see upstream `LICENSE`)
- **Revision**: commit `f1cd092727e3b69da606e95c055f80ad68404fc0` (2026-04-11)
- **Spec version**: `alpha` (`docs/spec.md` in the same repo, fetched the same day)
- **Vendored**: 2026-08-20, for ENH-3264 (little-loops DESIGN.md import support)

The upstream spec is `alpha` and may churn; if the fixture is refreshed, update the
commit SHA and date above. No content was altered — the file is copied byte-for-byte
so a diff against the upstream path reflects only upstream changes, not local edits.
