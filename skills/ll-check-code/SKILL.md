---
name: ll-check-code
description: Run code quality checks (lint, format, types, build)
metadata:
  short-description: Run code quality checks (lint, format, types, build)
---

# Check Code

Bridged from `commands/check-code.md` for Codex Skills API discovery.
See the source command file for the full prompt body.
