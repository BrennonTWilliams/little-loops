paradigm: imperative
name: codebase-scan
description: |
  Scan the codebase for new issues, verify discovered issues
  for accuracy, and assign priority levels.
steps:
  - "/ll:scan-codebase"
  - "/ll:verify-issues"
  - "/ll:prioritize-issues"
until:
  check: "git status --porcelain"
  condition: "output_empty"
max_iterations: 5
