---
id: ENH-2156
title: Fix stale skills count and phantom top-level loops/ directory in doc trees
type: ENH
priority: P3
status: open
created: 2026-06-14
affects:
- CONTRIBUTING.md
- docs/ARCHITECTURE.md
- README.md
confidence_score: 75
outcome_confidence: 70
score_complexity: 23
score_test_coverage: 20
score_ambiguity: 9
score_change_surface: 18
decision_needed: true
---

## Problem

Three documentation files contain a phantom top-level `loops/` directory that does not exist, plus stale skills counts:

**CONTRIBUTING.md:122** — Project tree shows:
```
├── loops/  # Built-in FSM loop definitions (94 YAML files) + yaml_state_editor Python module
```
This directory does not exist at the project root. Loops live at `scripts/little_loops/loops/` (89 runnable loops, 9 lib fragments). The `yaml_state_editor.py` module is already shown correctly under `scripts/little_loops/loops/` in the tree below.

**CONTRIBUTING.md:123** — Claims "38 skill definitions" but `ls skills/ | grep -v "^ll-" | wc -l` = 36 native skills. Also 6 native skills are missing from the tree listing: `adversarial-verify-loop`, `distill-traces`, `link-epics`, `rename-loop`, `simplify-loop`, `wire-issue`.

**docs/ARCHITECTURE.md:113-114** — Same phantom `loops/` appears:
```
├── loops/  # Built-in FSM loop definitions
├── skills/  # 38 skill definitions
```
The mermaid diagram (line 26) also shows `SKL[Skills<br/>37 composable skills]`.

**README.md:161** — `**38 skills**` (should be 36 native, or 66 total including Codex bridge adapters — decide canonical count).

## Acceptance Criteria

- [ ] Remove the phantom top-level `├── loops/` line from CONTRIBUTING.md tree
- [ ] Update CONTRIBUTING.md skills count to 36 and add the 6 missing skill entries to the tree
- [ ] Remove the phantom `├── loops/` from ARCHITECTURE.md tree; update `├── skills/ # 38` → `# 36`
- [ ] Update ARCHITECTURE.md mermaid diagram: `37 composable skills` → `36 composable skills`
- [ ] Update README.md: `38 skills` → `36 skills` (native) or decide on canonical count and document the distinction
- [ ] `ll-verify-docs` passes after changes (if it checks counts)

## Notes

The phantom `loops/` entry likely came from an early project layout that was later folded into `scripts/little_loops/loops/`. The counts drifted as skills were added during ENH-1730 (`link-epics`), ENH-2051 (`adversarial-verify-loop`), etc.

Canonical decision: "native skills" = skills authored in this repo (not Codex bridge adapters); the user-facing count should use native only (36) since bridge adapters are generated by `ll-adapt-skills-for-codex`.

## Confidence Check Notes

_Added by `/ll:confidence-check` on 2026-06-15_

**Readiness Score**: 75/100 → PROCEED WITH CAUTION
**Outcome Confidence**: 70/100 → MODERATE

### Concerns
- The proposed canonical count of "36 native skills" is internally inconsistent. `ls skills/ | grep -v "^ll-"` gives 36, but `ll-verify-docs` uses `BRIDGE_MARKER` exclusion and reports `actual=38`. The two "extra" skills are `ll-capture-issue` and `ll-go-no-go` — native skills with an `ll-` prefix that are not bridge adapters. Changing docs to "36" will cause `ll-verify-docs` to report documented=36 vs actual=38, violating the acceptance criterion "ll-verify-docs passes after changes."
- The acceptance criteria say nothing about updating `test_wiring_guides_and_meta.py`, which contains 5 assertions pinning the current (stale) counts: `"38 skills"` (README.md line 22), `"38 skill definitions"` (CONTRIBUTING.md lines 23, 150), `"37 composable skills"` (ARCHITECTURE.md line 31), `"# 38 skill definitions"` (ARCHITECTURE.md line 32). All of these will fail after the doc edits unless the test file is also updated.

### Gaps to Address
- Resolve the count conflict: native count should be **38** (not 36) per `ll-verify-docs` BRIDGE_MARKER logic, or update `doc_counts.py`'s exclusion logic to also exclude `ll-capture-issue` and `ll-go-no-go`. Document the decision.
- Add to acceptance criteria: update string-presence assertions in `test_wiring_guides_and_meta.py` lines 22, 23, 31, 32, 150 to match the new target counts.
- Resolve the README.md open question ("36 native, or 66 total including Codex bridge adapters — decide canonical count") before starting; this drives all other count targets.

### Outcome Risk Factors
- **Open decision on target count**: the issue proposes "36" but the verification tool reports actual=38 — implementing either value without resolving this open question will leave the docs or tool in a broken state.
- **Unmentioned test surface**: `test_wiring_guides_and_meta.py` pins the exact strings being changed; implementation will break 5 test assertions unless that file is included in scope.
- **Incomplete verification chain**: `ll-verify-docs` is cited for post-change verification, but it won't catch the test regressions introduced by the count change.

## Session Log
- `/ll:decide-issue` - 2026-06-15T14:38:40 - `9e9ec43e-4d7b-4807-bd3e-1b18b513a1f0.jsonl`
- `/ll:confidence-check` - 2026-06-15T00:00:00Z - `~/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/8884ba61-fa43-4473-b387-251b7e41b2d9.jsonl`
