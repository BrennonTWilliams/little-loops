---
id: FEAT-1526
type: FEAT
priority: P5
status: done
captured_at: '2026-05-16T00:00:00Z'
discovered_date: 2026-05-16
discovered_by: capture-issue
parent: EPIC-1463
decision_needed: false
confidence_score: 100
outcome_confidence: 74
score_complexity: 13
score_test_coverage: 18
score_ambiguity: 18
score_change_surface: 25
size: Very Large
---

# FEAT-1526: Adapt agents/*.md for Codex Subagents

## Summary

Codex CLI's subagent system (`.codex/agents/*.toml`, see
[developers.openai.com/codex/subagents](https://developers.openai.com/codex/subagents))
is the structural analog of Claude Code's `.claude/agents/*.md`. ll currently
ships only the Claude-shaped files, leaving Codex users without our custom
agents (`code-reviewer`, `Explore`, `Plan`, etc.). This issue adds a one-shot
adapter — `ll-adapt-agents-for-codex` — that generates `.codex/agents/<name>.toml`
from each `agents/*.md`, mirroring the established `ll-adapt-skills-for-codex`
(FEAT-1486) pattern.

## Current Behavior

- `agents/*.md` define ll's agents with Claude-shaped frontmatter
  (`name`, `description`, `tools`, optional `model`).
- Under Codex, `CodexRunner.build_streaming(agent=...)` emits a
  `CapabilityNotSupported` warning and drops the parameter. There is no native
  way for a Codex user to invoke ll-defined agents.
- `docs/codex/README.md` lists `--agent` as deferred.
- `docs/reference/HOST_COMPATIBILITY.md` shows `agent_select` for Codex as ✗.

## Expected Behavior

After this issue:
- Each `agents/<name>.md` has a corresponding `.codex/agents/<name>.toml`
  generated by `ll-adapt-agents-for-codex --apply`.
- Each TOML file contains `name`, `description`, `developer_instructions`
  (the agent body), and (when present in source) `model` / `mcp_servers`.
- Codex sessions in repos with `.codex/agents/` present can spawn ll-defined
  agents through Codex's normal in-conversation subagent mechanism.
- `docs/reference/HOST_COMPATIBILITY.md` updates Codex `agent_select` from
  ✗ to ✓ (or `(partial — model-spawned)` to flag the invocation-model
  difference).

## Motivation

This feature would:
- Close the last meaningful Claude/Codex parity gap in ll (skills and
  commands shipped via FEAT-1486/FEAT-1493; agents are the remaining hole).
- Give Codex users access to ll's curated agents (`Explore`, `Plan`,
  `code-reviewer`, ll-namespaced research agents) instead of falling back
  to Codex's three built-ins (`default`, `worker`, `explorer`).
- Follow the established cross-host pattern in this repo: **generate
  per-host native files from a single source-of-truth**, rather than
  attempting to translate at the CLI invocation layer (which is structurally
  impossible — Codex spawns subagents model-internally, not via flag).
- Make `CodexRunner`'s "agent dropped with warning" behavior actionable —
  the warning text already points at this adapter.

## Use Case

**Who**: An ll user whose primary host is Codex CLI, working in a repo that
defines custom agents in `.claude/agents/` or `agents/`.

**Context**: They open a Codex session and ask "use the code-reviewer agent
to review this diff". Today, nothing happens — Codex has never heard of
`code-reviewer`.

**Goal**: Codex's orchestrator recognizes ll-defined agents and spawns them
when contextually appropriate.

**Outcome**: After running `ll-adapt-agents-for-codex --apply` once,
`.codex/agents/code-reviewer.toml` exists and Codex spawns the agent during
relevant tasks.

## Acceptance Criteria

- [ ] New CLI `ll-adapt-agents-for-codex` (dry-run default, `--apply` writes)
- [ ] Walks `agents/*.md` and generates `.codex/agents/<name>.toml` for each
- [ ] TOML fields: `name` (slug), `description` (single-line), `developer_instructions`
      (full body after frontmatter), `model` if present in source, `mcp_servers`
      if present in source
- [ ] Idempotent — re-running produces no diff if source unchanged
- [ ] Existing `.codex/agents/<name>.toml` content is preserved when not derived
      from an `agents/*.md` source (user-authored files are not clobbered);
      a marker comment (`# generated by ll-adapt-agents-for-codex`) identifies
      managed files
- [ ] `docs/reference/HOST_COMPATIBILITY.md` Codex `agent_select` cell updated
      from ✗ to ✓ (or `(partial — model-spawned)`)
- [ ] `docs/codex/README.md` "What is deferred" entry for `--agent` rewritten
      to point at the adapter
- [ ] `CodexRunner` capability message and docstring (already updated in this
      session) are consistent with the new behavior
- [ ] Tests: unit (frontmatter parsing, TOML emission, idempotency, marker
      preservation) + integration guard (real `agents/` dir round-trips cleanly)

## Research Notes

- **Established pattern in this repo**: `scripts/little_loops/cli/adapt_skills_for_codex.py`
  is the canonical model — three-file CLI registration, dry-run by default,
  `_find_plugin_root()` for source discovery, additive non-destructive writes.
- **Codex subagent TOML schema**: per
  [developers.openai.com/codex/subagents](https://developers.openai.com/codex/subagents),
  fields are `name`, `description`, `developer_instructions`, optional
  `model`, optional `sandbox_mode`, optional `mcp_servers`. The body of the
  Claude-shaped `agents/*.md` (after frontmatter) maps to `developer_instructions`.
- **Invocation model gap (cannot be closed)**: Claude Code dispatches sub-agents
  at the platform level via the Agent tool (`subagent_type: <name>`); Codex
  spawns subagents model-internally during a conversation. This adapter closes
  the *definition* gap but the *invocation* gap remains — callers passing
  `agent=` to `CodexRunner` still get a warning + drop, because Codex provides
  no mechanism to pre-select a subagent for a one-shot `codex exec`. This is
  consistent with how every surveyed cross-host plugin handles it (see prior-art
  notes below).
- **Prior art surveyed**:
  - [EveryInc/compound-engineering-plugin](https://github.com/EveryInc/compound-engineering-plugin)
    — ships separate per-host agent files via a Bun installer step. Confirms
    "generate native files, don't translate at runtime" is the pragmatic pattern.
  - [mco-org/mco](https://github.com/mco-org/mco) — neutral orchestration layer
    that owns its own agent registry and writes per-host adapters. More
    architecture than ll needs; we already own `agents/*.md` as source-of-truth.
  - `claude-to-codex` and `sync-claude-skills-to-codex` community tools
    explicitly handle *skills only*, not agents.
- **What we don't do**: prompt-injection of the persona's `developer_instructions`
  into the user prompt prefix. Tempting because it would honor `agent=` under
  Codex, but: (a) collides with Codex's native subagent spawning once TOML files
  exist, (b) loses tool-allowlist semantics entirely, (c) diverges agent
  behavior between hosts in invisible ways. Rejected.

## Integration Map

### Files to Create

- `scripts/little_loops/cli/adapt_agents_for_codex.py` — new CLI module with
  `main_adapt_agents_for_codex()`
- `scripts/tests/test_adapt_agents_for_codex.py` — unit + integration guard tests

### Files to Modify

- `scripts/little_loops/cli/__init__.py` — re-export `main_adapt_agents_for_codex`
- `scripts/pyproject.toml` — register `ll-adapt-agents-for-codex` under
  `[project.scripts]`
- `docs/reference/HOST_COMPATIBILITY.md` — flip Codex `agent_select` cell
- `docs/codex/README.md` — rewrite the `--agent` deferred entry
- `docs/codex/usage.md` — document the new CLI under "Current Limitations" or
  a new "Adapting Claude artifacts for Codex" section
- `docs/reference/CLI.md` — add `### ll-adapt-agents-for-codex` section
- `commands/help.md` — add one-line entry in CLI TOOLS block
- `.claude/CLAUDE.md` "CLI Tools" section — add one-line entry
- `skills/configure/areas.md` — increment authorize count and append CLI name
- `README.md` — increment CLI tool counts (two separate count strings: `"24 typed CLI tools"` at line 46 and `"25 CLI tools"` at line 166 — both need incrementing)

_Wiring pass added by `/ll:wire-issue`:_
- `scripts/tests/test_create_extension_wiring.py` — 4 stale count assertions must be updated simultaneously with doc changes: `TestConfigureAreasWiring.test_count_updated_to_17` and `TestFeat1229LlActionWiring.test_configure_areas_count_is_17` assert `"Authorize all 22"` (→ 23); `TestFeat1045DocUpdates.test_readme_tool_count_is_20` and `TestFeat1229LlActionWiring.test_readme_tool_count_is_20` assert `"24 typed CLI tools"` (→ 25); also add new `TestFeat1526LlAdaptAgentsWiring` doc wiring class following `TestFeat1486LlAdaptSkillsWiring` as model [Agent 2/3 finding]
- `scripts/tests/test_ll_logs_wiring.py` — asserts `"Authorize all 22"`, will fail when `areas.md` moves to 23 [Agent 3 finding]
- `scripts/tests/test_feat1504_doc_wiring.py` — asserts `"Authorize all 22"`, will fail when `areas.md` moves to 23 [Agent 3 finding]
- `docs/codex/getting-started.md` — "Skill and command discovery" section documents `ll-adapt-skills-for-codex --apply` as a post-install step; add `ll-adapt-agents-for-codex --apply` as a companion step [Agent 2 finding]
- `CONTRIBUTING.md` — "Adding Agents" section has no post-creation adapter step; add one parallel to the "After Creating a New Command" workflow referencing `ll-adapt-agents-for-codex --apply` [Agent 2 finding]
- `scripts/little_loops/host_runner.py` — `CodexRunner` class docstring references "a future `ll-adapt-agents-for-codex`"; update qualifier once CLI ships [Agent 2 finding]
- `scripts/little_loops/cli/__init__.py` module docstring (lines 1–28) — the docstring enumerates every `ll-*` tool and is out-of-sync if only `__all__` is updated; add `ll-adapt-agents-for-codex` entry to the docstring catalog as well [Agent 2 finding]

### Files Generated (not committed by default)

- `.codex/agents/<name>.toml` per source `agents/<name>.md` — these may or
  may not be committed depending on whether the repo treats Codex artifacts
  as source or generated. Recommend: commit them so users get them without
  re-running the adapter. Match the `skills/*/agents/openai.yaml` convention
  established by FEAT-1486.

### Dependent Files

- `scripts/little_loops/host_runner.py` — `CodexRunner` docstring + warning
  text already reference this CLI (updated in the session that filed this
  issue). When the CLI ships, no further `host_runner.py` change is needed;
  the warning still fires (correctly — `agent=` still can't be honored at
  invocation), but now the message has a concrete action for users.
- `scripts/little_loops/skill_expander.py` — contains the real `_find_plugin_root()`
  implementation. `adapt_skills_for_codex.py` wraps it via `from little_loops.skill_expander
  import _find_plugin_root as _fpr`; the new module must do the same rather than
  duplicating the path-resolution logic.

### Similar Patterns

- `scripts/little_loops/cli/adapt_skills_for_codex.py` — direct template;
  same dry-run pattern, same `_find_plugin_root()` usage, same three-file
  registration. Differences: TOML output instead of YAML frontmatter mutation,
  separate target directory (`.codex/agents/`) instead of in-place edits.

### Codebase Research Findings

_Added by `/ll:refine-issue` — based on codebase analysis:_

- **`__init__.py` registration**: import at line 31 (`from little_loops.cli.adapt_skills_for_codex import ...`); add to `__all__` list starting at line 58. New entry follows the same alphabetical grouping.
- **`pyproject.toml` registration**: line 73 is the `ll-adapt-skills-for-codex` entry (the direct model). Add `ll-adapt-agents-for-codex = "little_loops.cli:main_adapt_agents_for_codex"` after line 74 (`ll-learning-tests`).
- **`tomli_w` not yet a dependency**: `pyproject.toml` lines 37-40 show only `pyyaml>=6.0` and `wcwidth>=0.2` as runtime dependencies. `tomli_w` must be added. Alternative: since the TOML output is structurally simple (flat keys + one multiline string), raw f-strings work for all required fields — `adapt_skills_for_codex.py` uses this approach for all YAML output already.
- **Agents glob pattern**: `agents_dir.glob("*.md")` (flat directory, not `*/SKILL.md`). Agent name is `agent_md.stem`, not `agent_md.parent.name`.
- **Frontmatter field reality** (confirmed across all 8 `agents/*.md`): every file has `name`, `description` (always block scalar `|`), `model` (`sonnet` or `haiku`), and `tools` (JSON array on one line, e.g., `["Read", "Glob", "Grep"]`). No current agent uses `mcp_servers`, `sandbox_mode`, `background`, or `isolation`.
- **Test template**: `scripts/tests/test_adapt_skills_for_codex.py` — classes `TestExtractShortDesc`, `TestInsertFields`, `TestMakeOpenaiYaml`, `TestProcessSkills`, `TestMainAdaptSkillsForCodex`, `TestRealSkillsIntegrationGuard`. For agents: the integration guard (`TestRealAgentsIntegrationGuard`) uses `Path(__file__).parent.parent.parent / "agents"` and returns early (not skips) when `.codex/agents/` doesn't yet exist.
- **Idempotency test pattern** (from `test_generate_schemas.py:TestGenerateSchemas.test_idempotent_on_second_run`): call `_process_agents(..., apply=True)` twice, snapshot `.toml` contents after each call, assert they match byte-for-byte.

### Tests

_Wiring pass added by `/ll:wire-issue`:_
- `scripts/tests/test_adapt_agents_for_codex.py` — new test file (already in Files to Create); mirror `test_adapt_skills_for_codex.py` with classes: `TestExtractAgentFrontmatter`, `TestEmitAgentToml`, `TestProcessAgents`, `TestMainAdaptAgentsForCodex`, `TestRealAgentsIntegrationGuard`, `TestIdempotency`, `TestMarkerPreservation` [Agent 3 finding]
- `scripts/tests/test_create_extension_wiring.py` — 4 existing test methods with stale count assertions will fail when `areas.md` and `README.md` counts increment; must be updated simultaneously with the doc changes [Agent 3 finding]
- `scripts/tests/test_ll_logs_wiring.py` — existing `"Authorize all 22"` assertion fails when `areas.md` moves to 23 [Agent 3 finding]
- `scripts/tests/test_feat1504_doc_wiring.py` — existing `"Authorize all 22"` assertion fails when `areas.md` moves to 23 [Agent 3 finding]

### Documentation

_Wiring pass added by `/ll:wire-issue`:_
- `docs/codex/getting-started.md` — "Skill and command discovery" section documents `ll-adapt-skills-for-codex --apply` as a post-install step; add `ll-adapt-agents-for-codex --apply` as a companion step [Agent 2 finding]
- `CONTRIBUTING.md` — "Adding Agents" section currently has no post-creation adapter step; add one parallel to "After Creating a New Command" workflow referencing `ll-adapt-agents-for-codex --apply` [Agent 2 finding]

## Implementation Steps

1. Audit `agents/*.md` to enumerate sources and identify frontmatter fields
   in use (`name`, `description`, `tools`, `model`).
2. Write `scripts/little_loops/cli/adapt_agents_for_codex.py`:
   - Walk `agents/*.md` via `agents_dir.glob("*.md")`; name = `agent_md.stem`.
   - Delegate to `skill_expander._find_plugin_root` (same shim pattern as
     `adapt_skills_for_codex.py`).
   - Parse frontmatter with `yaml.safe_load`; extract `description` (block
     scalar — use `_extract_short_desc` logic to get first non-empty line for
     the TOML `description` field; full body after `---` → `developer_instructions`).
   - Emit TOML via `tomli_w` (stdlib `tomllib` is read-only) **or** f-strings
     (viable because output is structurally simple — flat keys + one multiline
     string); if f-strings, no new dependency needed.
   - Write to `<repo-root>/.codex/agents/<name>.toml` with `# generated by
     ll-adapt-agents-for-codex` at the first line for idempotency detection.
   - Dry-run mode prints what would change; `--apply` writes files.
3. Register CLI (three-file pattern):
   - `scripts/little_loops/cli/__init__.py`: import at line 31 block + add to `__all__` at line 58 block.
   - `scripts/pyproject.toml`: add entry after line 74 (`ll-learning-tests = ...`).
4. Run adapter against the real `agents/` dir and commit the generated
   `.codex/agents/*.toml` files.
5. Update docs (HOST_COMPATIBILITY, codex/README, codex/usage, CLI.md, help.md,
   CLAUDE.md, areas.md, README counts).
6. Write tests modeled on `test_adapt_skills_for_codex.py`.
7. Manual smoke test: in a Codex session, ask the model to use one of the
   ll-defined agents and verify it spawns.

### Wiring Phase (added by `/ll:wire-issue`)

_These touchpoints were identified by wiring analysis and must be included in the implementation:_

8. Update `scripts/tests/test_create_extension_wiring.py` — (a) update 4 stale count assertions (`"Authorize all 22"` → `"Authorize all 23"` in `TestConfigureAreasWiring.test_count_updated_to_17` and `TestFeat1229LlActionWiring.test_configure_areas_count_is_17`; `"24 typed CLI tools"` → `"25 typed CLI tools"` in `TestFeat1045DocUpdates.test_readme_tool_count_is_20` and `TestFeat1229LlActionWiring.test_readme_tool_count_is_20`); (b) add `TestFeat1526LlAdaptAgentsWiring` class — follow `TestFeat1486LlAdaptSkillsWiring` as the direct model, checking `help.md`, `.claude/CLAUDE.md`, `CLI.md`, `areas.md`, and Codex docs for the new CLI name
9. Update `scripts/tests/test_ll_logs_wiring.py` and `scripts/tests/test_feat1504_doc_wiring.py` — change `"Authorize all 22"` → `"Authorize all 23"` in both files, alongside the `areas.md` change in step 5
10. Update `docs/codex/getting-started.md` — add `ll-adapt-agents-for-codex --apply` as companion step after the existing `ll-adapt-skills-for-codex --apply` step in "Skill and command discovery"
11. Update `CONTRIBUTING.md` "Adding Agents" section — add post-creation adapter workflow note referencing `ll-adapt-agents-for-codex --apply`
12. Update `scripts/little_loops/host_runner.py` `CodexRunner` class docstring — replace "a future `ll-adapt-agents-for-codex`" with the concrete CLI reference once shipped
13. Update `scripts/little_loops/cli/__init__.py` module docstring (lines 1–28) — add `ll-adapt-agents-for-codex` entry alongside the `__all__` export update

## API/Interface

```bash
# Default: dry-run, prints generated TOML to stdout
ll-adapt-agents-for-codex

# Write .codex/agents/<name>.toml
ll-adapt-agents-for-codex --apply

# Restrict to a single agent
ll-adapt-agents-for-codex --apply --only code-reviewer
```

Python: `main_adapt_agents_for_codex() -> int` returning a process exit code.

## Impact

- **Priority**: P5 — Codex host is exploratory; Claude remains primary. No
  user is blocked but the gap is the last meaningful one in our cross-host
  story.
- **Effort**: Small-to-medium — the structural template exists
  (`adapt_skills_for_codex.py`); main novelty is TOML emission and the
  `.codex/agents/` target directory.
- **Risk**: Low — additive; generated files live in `.codex/agents/`,
  independent of `agents/*.md` sources; idempotent; no existing invocation
  path changes; Codex users opt in by running the CLI.
- **Breaking Change**: No.

## Labels

codex, agents, host-compat

## Status

**Open** | Created: 2026-05-16 | Priority: P5


## Confidence Check Notes

_Added by `/ll:confidence-check` on 2026-05-16_

**Readiness Score**: 100/100 → PROCEED
**Outcome Confidence**: 74/100 → MODERATE

### Outcome Risk Factors
- **Broad enumeration across 19 change sites**: Complexity Breadth scored 0/12 (16+ sites). All sites are explicitly listed in the Integration Map, but coordinating 19 simultaneous changes introduces execution overhead. Use the Implementation Steps sequence as a strict checklist.
- **Wiring test count assertion synchronization**: 6 test methods across 3 files (`test_create_extension_wiring.py` ×4, `test_ll_logs_wiring.py` ×1, `test_feat1504_doc_wiring.py` ×1) assert `"Authorize all 22"` or `"24 typed CLI tools"`. These must be updated in the same pass as `areas.md` and `README.md` or the test suite will fail. Steps 8-9 must execute atomically with step 5.

---

## Resolution

- **Status**: Decomposed
- **Completed**: 2026-05-16
- **Reason**: Issue too large for single session (score 11/11 — Very Large)

### Decomposed Into
- FEAT-1527: Core CLI — ll-adapt-agents-for-codex implementation and tests
- FEAT-1528: Documentation and wiring integration for ll-adapt-agents-for-codex

## Session Log
- `/ll:confidence-check` - 2026-05-16T00:00:00Z - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/a1c9c8e0-d04b-464f-b650-e498785322de.jsonl`
- `/ll:wire-issue` - 2026-05-16T20:56:26 - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/a1c9c8e0-d04b-464f-b650-e498785322de.jsonl`
- `/ll:refine-issue` - 2026-05-16T20:50:04 - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/cf0e2b8c-4304-4ef6-ba50-b0ba3e4305ce.jsonl`
- `/ll:issue-size-review` - 2026-05-16T21:00:00Z - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/0de5eba4-6630-4b6f-ac14-f65397d900bb.jsonl`
