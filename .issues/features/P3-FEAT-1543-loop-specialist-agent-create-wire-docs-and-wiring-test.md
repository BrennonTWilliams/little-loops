---
id: FEAT-1543
priority: P3
type: FEAT
parent: FEAT-1532
size: Large
status: done
completed_at: 2026-05-17T06:29:13Z
confidence_score: 100
outcome_confidence: 86
score_complexity: 18
score_test_coverage: 18
score_ambiguity: 25
score_change_surface: 25
---

# FEAT-1543: Create loop-specialist agent, wiring, docs, and doc-wiring test

## Summary

Create `agents/loop-specialist.md`, register it in all required locations (plugin.json, .gitignore, codex TOML), update all count-bearing documentation strings from 8→9, create the `## Agents` section in `docs/reference/API.md`, and write the doc-wiring test `test_feat1532_doc_wiring.py`. All changes must ship in a single atomic commit to avoid triggering the `docs-sync.yaml` loop's auto-repair state.

## Parent Issue
Decomposed from FEAT-1532: agent-loop-specialist monitors, analyzes, refines, and optimizes FSM loops

## Context

See FEAT-1532 for the full agent design, failure-mode taxonomy, diagnosis artifact format, verification approach, and codebase research findings. This child covers everything except the behavioral eval (that is FEAT-1544).

The agent prompt must embed the failure-mode taxonomy (ambiguous output / infinite cycle / premature termination / feature-stubbing / drift / self-evaluation bias) and the diagnosis artifact format (`.loops/diagnostics/<loop>-<ts>.md`).

Key codebase research from parent (must read before implementing):
- Correct frontmatter schema: see `agents/consistency-checker.md:1-31` and `agents/plugin-config-auditor.md:1-32` — no `triggers:` YAML key; trigger phrases live as prose inside `description:`
- `ll-adapt-agents-for-codex` auto-discovers `agents/*.md`; generated `.codex/agents/loop-specialist.toml` will have `# generated by ll-adapt-agents-for-codex` on line 1
- Verification step uses `ll-loop run <name> --max-iterations 1` (real LLM); `ll-loop simulate --scenario` uses `SimulationActionRunner` returning synthetic strings and cannot verify content predicates
- `ll-loop history <name>` requires `--json` flag for machine-parseable output; same for `ll-loop status --json`
- `.claude-plugin/plugin.json` agents array is explicit (lines 21-29) — must add `"./agents/loop-specialist.md"`

## Acceptance Criteria

- [ ] `agents/loop-specialist.md` exists with correct frontmatter (name, description multi-line block, model, tools JSON-array)
- [ ] Agent description embeds the 6 failure-mode taxonomy entries
- [ ] Agent instructions reference diagnosis artifact path `.loops/diagnostics/<loop>-<ts>.md`
- [ ] Agent instructions specify `ll-loop history <name> --json` and `ll-loop status --json` for machine-parseable output
- [ ] `.claude-plugin/plugin.json` agents array includes `"./agents/loop-specialist.md"`
- [ ] `.gitignore` has `.loops/diagnostics/` entry (alongside `.loops/tmp/`)
- [ ] `ll-adapt-agents-for-codex --apply` generates `.codex/agents/loop-specialist.toml` with `# generated by ll-adapt-agents-for-codex` on line 1
- [ ] `README.md` says "9 specialized agents" (was 8)
- [ ] `CONTRIBUTING.md` count updated to 9 and tree includes `│   ├── loop-specialist.md`
- [ ] `docs/ARCHITECTURE.md` Mermaid node and count updated to 9; tree includes `loop-specialist.md`
- [ ] `docs/reference/API.md` has new `## Agents` section listing all 9 agents with descriptions
- [ ] `test_feat1532_doc_wiring.py` passes: agent file exists with required frontmatter, TOML exists with marker, README/CONTRIBUTING/ARCHITECTURE say "9", `plugin.json` contains `"./agents/loop-specialist.md"`, API.md contains "loop-specialist"
- [ ] All changes above are in a single atomic commit (prevents `docs-sync.yaml` auto-repair from firing)

## Implementation Steps

1. Read `agents/consistency-checker.md` and `agents/plugin-config-auditor.md` to confirm exact frontmatter schema
2. Create `agents/loop-specialist.md` with the monitor → analyze → contract → refine → verify → improve → optimize instructions; embed the failure-mode taxonomy table and diagnosis artifact format inline; use `tools: ["Bash", "Read", "Edit", "Write"]` JSON-array syntax
3. Add `.loops/diagnostics/` to `.gitignore` after `.loops/tmp/` line
4. Add `"./agents/loop-specialist.md"` to the `"agents"` array in `.claude-plugin/plugin.json` (lines 21-29)
5. Write `scripts/tests/test_feat1532_doc_wiring.py` — follow `test_feat1462_doc_wiring.py` pattern; include:
   - `TestAgentFileWiring`: agent file exists, has required frontmatter fields, toml exists with marker
   - `TestDocCountWiring`: README/CONTRIBUTING/ARCHITECTURE each contain "9 specialized agents" (or equivalent)
   - `TestApiMdWiring`: `docs/reference/API.md` contains "loop-specialist"
   - `TestPluginJsonWiring`: `"./agents/loop-specialist.md"` in plugin.json agents array
6. Run `ll-adapt-agents-for-codex --apply` and verify `.codex/agents/loop-specialist.toml` is generated
7. Update count references 8 → 9 in: `README.md:164`, `CONTRIBUTING.md:111`; add `│   ├── loop-specialist.md` to the tree listing at `CONTRIBUTING.md:119` and `docs/ARCHITECTURE.md:79`; update `docs/ARCHITECTURE.md:25` Mermaid node and line 71 count
8. Create `## Agents` section in `docs/reference/API.md` listing all 9 agents with descriptions
9. Run the new test file: `python -m pytest scripts/tests/test_feat1532_doc_wiring.py -v`
10. Stage and commit all changes atomically: `agents/loop-specialist.md`, `.codex/agents/loop-specialist.toml`, `.claude-plugin/plugin.json`, `.gitignore`, `README.md`, `CONTRIBUTING.md`, `docs/ARCHITECTURE.md`, `docs/reference/API.md`, `scripts/tests/test_feat1532_doc_wiring.py`

### Wiring Phase (added by `/ll:wire-issue`)

_These touchpoints were identified by wiring analysis and must be included in the implementation:_

11. After Step 6 (`ll-adapt-agents-for-codex --apply`), run `python -m pytest scripts/tests/test_adapt_agents_for_codex.py -k TestRealAgentsIntegrationGuard -v` to confirm the integration guard passes — this test iterates real `agents/*.md` and fails if `loop-specialist.toml` is missing from `.codex/agents/`

## Files to Create/Modify

- `agents/loop-specialist.md` — new (create)
- `.codex/agents/loop-specialist.toml` — new (generated by ll-adapt-agents-for-codex)
- `.claude-plugin/plugin.json` — add to agents array
- `.gitignore` — add `.loops/diagnostics/`
- `README.md:164` — 8 → 9 agents count
- `CONTRIBUTING.md:111` — count; `:119` — tree entry
- `docs/ARCHITECTURE.md:25,71,79` — Mermaid + count + tree
- `docs/reference/API.md` — new `## Agents` section
- `scripts/tests/test_feat1532_doc_wiring.py` — new wiring test

## Integration Map

### Codebase Research Findings

_Added by `/ll:refine-issue` — based on codebase analysis:_

#### Files to Create
- `agents/loop-specialist.md` — frontmatter schema (confirmed from `agents/consistency-checker.md:1-31`): `name: loop-specialist`, `description: |` (pipe-literal multiline block with `<example>` XML blocks + "When NOT to use" + `Trigger:` line), `model: sonnet`, `tools: ["Bash", "Read", "Edit", "Write"]`
- `.codex/agents/loop-specialist.toml` — generated by `scripts/little_loops/cli/adapt_agents_for_codex.py:_emit_agent_toml()`; marker `# generated by ll-adapt-agents-for-codex` on line 1; `description` field = first ~80 chars of frontmatter description; `developer_instructions` = triple-quoted body text
- `scripts/tests/test_feat1532_doc_wiring.py` — follow `scripts/tests/test_feat1462_doc_wiring.py` pattern exactly: `PROJECT_ROOT = Path(__file__).parent.parent.parent`, module-level path constants, one `class Test<Surface>Wiring` per surface, each test reads content and asserts token `in content`

#### Files to Modify (with exact edit context)
- `.claude-plugin/plugin.json:25` — insert `"./agents/loop-specialist.md",` after the `consistency-checker.md` line (line 25); alphabetical position: l > c, l < p
- `.gitignore:79` — insert `.loops/diagnostics/` after `.loops/tmp/` (line 79); line 80 starts a new pattern group (`*.Cast`), so insert before it
- `README.md:164` — change `**8 specialized agents**` → `**9 specialized agents**`
- `CONTRIBUTING.md:111` — change `# 8 agent definitions (*.md)` → `# 9 agent definitions (*.md)`
- `CONTRIBUTING.md:115` — insert `│   ├── loop-specialist.md` after this line (`│   ├── consistency-checker.md`); this is the correct alphabetical position (between `consistency-checker` and `plugin-config-auditor`)
- `docs/ARCHITECTURE.md:25` — change `AGT[Agents<br/>8 specialized agents]` → `AGT[Agents<br/>9 specialized agents]`
- `docs/ARCHITECTURE.md:71` — change `# 8 specialized agents` → `# 9 specialized agents`
- `docs/ARCHITECTURE.md:75` — insert `│   ├── loop-specialist.md` after this line (`│   ├── consistency-checker.md`)
- `docs/reference/API.md` — append `## Agents` section at end of file (after `## little_loops.skill_expander` which ends the file); list all 9 agents with one-line descriptions

#### Note on Tree Insertion (Step 7 clarification)
Step 7 references `CONTRIBUTING.md:119` and `ARCHITECTURE.md:79` as insertion lines, but those are `│   └── workflow-pattern-analyzer.md` (current last entries). Alphabetically, `loop-specialist` (l) belongs between `consistency-checker.md` and `plugin-config-auditor.md`. The correct insert-after lines are:
- CONTRIBUTING.md: after line 115 (`│   ├── consistency-checker.md`)
- ARCHITECTURE.md: after line 75 (`│   ├── consistency-checker.md`)

#### Wiring Test Method Guide (`test_feat1532_doc_wiring.py`)
```python
PROJECT_ROOT = Path(__file__).parent.parent.parent
AGENT_FILE = PROJECT_ROOT / "agents" / "loop-specialist.md"
TOML_FILE = PROJECT_ROOT / ".codex" / "agents" / "loop-specialist.toml"
README = PROJECT_ROOT / "README.md"
CONTRIBUTING = PROJECT_ROOT / "CONTRIBUTING.md"
ARCHITECTURE = PROJECT_ROOT / "docs" / "ARCHITECTURE.md"
API_DOC = PROJECT_ROOT / "docs" / "reference" / "API.md"
PLUGIN_JSON = PROJECT_ROOT / ".claude-plugin" / "plugin.json"
```

`TestAgentFileWiring`:
- `test_agent_file_exists` → `assert AGENT_FILE.exists()`
- `test_agent_has_name_field` → `assert "name: loop-specialist" in content`
- `test_agent_has_model_field` → `assert "model:" in content`
- `test_agent_has_tools_field` → `assert "tools:" in content`
- `test_toml_exists` → `assert TOML_FILE.exists()`
- `test_toml_has_generated_marker` → `assert content.startswith("# generated by ll-adapt-agents-for-codex")`

`TestDocCountWiring`:
- `test_readme_says_9` → `assert "9 specialized agents" in README.read_text()`
- `test_contributing_says_9` → `assert "9 agent definitions" in CONTRIBUTING.read_text()`
- `test_architecture_says_9` → `assert "9 specialized agents" in ARCHITECTURE.read_text()`

`TestApiMdWiring`:
- `test_api_md_has_loop_specialist` → `assert "loop-specialist" in API_DOC.read_text()`

`TestPluginJsonWiring`:
- `test_plugin_json_has_agent` → `assert '"./agents/loop-specialist.md"' in PLUGIN_JSON.read_text()`

### Dependent Files (Callers/Importers)

_Wiring pass added by `/ll:wire-issue`:_
- `scripts/little_loops/doc_counts.py` — `verify_documentation()` / `count_files()` dynamically scans `agents/*.md` at runtime; once `loop-specialist.md` exists, `ll-verify-docs` will report count mismatches until README/CONTRIBUTING/ARCHITECTURE are updated to "9". The atomic commit approach in Step 10 prevents intermediate CI failures.

### Tests

_Wiring pass added by `/ll:wire-issue`:_
- `scripts/tests/test_adapt_agents_for_codex.py` — **existing test to run after Step 6**: `TestRealAgentsIntegrationGuard.test_all_real_agents_have_toml_files` iterates `agents/*.md` and asserts each has a corresponding `.codex/agents/<stem>.toml`. Creating `agents/loop-specialist.md` (Step 2) before running `ll-adapt-agents-for-codex --apply` (Step 6) will cause this test to fail. The full test suite should not be run between these two steps.

## Related

- Parent: FEAT-1532
- Next child (eval): FEAT-1544

## Session Log
- `/ll:manage-issue` - 2026-05-17T06:29:13Z - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/3526a99b-7c49-4c45-a664-5761207b3ac3.jsonl`
- `/ll:ready-issue` - 2026-05-17T06:24:47 - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/f9c6bdcc-2612-46ee-ac30-a892519faf1a.jsonl`
- `/ll:wire-issue` - 2026-05-17T06:20:35 - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/e3a42f68-91fc-4ab8-acb8-7b61ced8deb7.jsonl`
- `/ll:refine-issue` - 2026-05-17T06:13:43 - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/828ca570-21ca-4627-9028-02bbc7f9df52.jsonl`
- `/ll:issue-size-review` - 2026-05-17T00:00:00Z - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/1f2aa363-89b4-48fb-b7e2-882be8ac2cc8.jsonl`
- `/ll:confidence-check` - 2026-05-17T07:00:00Z - `/Users/brennon/.claude/projects/-Users-brennon-AIProjects-brenentech-little-loops/a2014a17-48ef-42b3-af50-4be647f7894a.jsonl`

---

**Done** | Created: 2026-05-17 | Completed: 2026-05-17 | Priority: P3

## Resolution

Created `agents/loop-specialist.md` with the failure-mode taxonomy (ambiguous-output, infinite-cycle, premature-termination, feature-stubbing, drift, self-evaluation bias), the seven-step monitor→optimize workflow, and the `.loops/diagnostics/<loop>-<ts>.md` artifact format. All count-bearing docs (README, CONTRIBUTING, ARCHITECTURE) updated 8→9 and tree listings include the new agent. `docs/reference/API.md` now has an `## Agents` section listing all 9 agents. `ll-adapt-agents-for-codex --apply` produced `.codex/agents/loop-specialist.toml`. New `scripts/tests/test_feat1532_doc_wiring.py` (12 assertions) and the pre-existing `TestRealAgentsIntegrationGuard` (3 assertions) all pass. Shipped as a single atomic commit so the `docs-sync.yaml` loop has no transient mismatch state to repair.
