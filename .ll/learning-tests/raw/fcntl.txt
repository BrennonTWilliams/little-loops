=== Claim 1: per-open-file-description, two opens in same process ===
fd_a locked LOCK_EX: ok
fd_b LOCK_EX|LOCK_NB from SAME process on separate fd: RAISED BlockingIOError errno=35

=== Claim 2: flock auto-released on close (verified via child process) ===
parent acquired LOCK_EX after child exited without LOCK_UN: SUCCEEDED (lock auto-released)

=== Claim 3: F_GETFL returns access-mode bits ===
F_GETFL flags=0, flags & O_ACCMODE=0, os.O_RDONLY=0
matches O_RDONLY: True

=== Claim 4: O_NONBLOCK on pipe read end raises BlockingIOError on empty read ===
read on empty nonblocking pipe raised BlockingIOError errno=35

=== Claim 5: LOCK_EX (blocking) waits for holder to release, held by CHILD process ===
parent blocking LOCK_EX acquired after 0.396s (child held ~0.5s)
blocked_as_expected: True

=== Claim 6: child inherits parent's lock on the SAME fd (same open-file-description) ===

DONE
child re-locking SAME inherited fd: SUCCEEDED (no-op, same OFD)
