=== Claim: gh issue list --json outputs valid JSON array ===
exit: 0
parsed OK, type: list len: 0

=== Claim: gh issue create --help has no --json flag ===
False

=== Claim: gh api repos/{owner}/{repo} has default_branch key ===
exit: 0
has default_branch: True value: main

=== Claim: gh auth status exits 0 when authenticated ===
exit: 0

=== Claim: gh issue edit --help lists --add-label flag ===
True

=== Claim: gh pr view --help lists --json flag with state in field list ===
True

=== Follow-up: gh pr view --json field list (verifying 'state' is a valid field) ===
JSON FIELDS include: state (confirmed via `gh pr view --help`)

=== 2026-09-04 (ENH-3205 review) gh version 2.86.0 (2026-01-21), macOS, login source: keyring ===

=== Claim: GH_CONFIG_DIR=<empty dir> hides keyring login (GH_TOKEN/GITHUB_TOKEN unset) ===
$ env -u GH_TOKEN -u GITHUB_TOKEN GH_CONFIG_DIR=$EMPTY gh auth status
You are not logged into any GitHub hosts. To log in, run: gh auth login
exit: 0
$ env -u GH_TOKEN -u GITHUB_TOKEN GH_CONFIG_DIR=$EMPTY gh api user -q .login
To get started with GitHub CLI, please run:  gh auth login
Alternatively, populate the GH_TOKEN environment variable with a GitHub API authentication token.
exit: 4

=== Claim: gh auth token yields ambient token with no env var set ===
$ gh auth token | wc -c
exit: 0, len: 40 (value not recorded)

=== 2026-09-08 (BUG-3402 fix verification) gh version 2.86.0 (2026-01-21), macOS, login source: keyring ===

=== Claim: GH_TOKEN=invalid sentinel + GH_CONFIG_DIR=<empty dir> -> gh auth status fails ===
$ GH_CONFIG_DIR=$EMPTY GH_TOKEN=ll-scoped-no-github-token gh auth status
github.com
  X Failed to log in to github.com using token (GH_TOKEN)
  - Active account: true
  - The token in GH_TOKEN is invalid.
exit: 1

=== Claim: GH_TOKEN=invalid sentinel + GH_CONFIG_DIR=<empty dir> -> gh auth token returns the sentinel, not the real Keychain token ===
$ GH_CONFIG_DIR=$EMPTY GH_TOKEN=ll-scoped-no-github-token gh auth token
ll-scoped-no-github-token
exit: 0

=== 2026-09-08 (BUG-3402 fix, nested case + ambient residual re-check) gh version 2.86.0 (2026-01-21), macOS, login source: keyring ===

=== Claim: GH_TOKEN=<sentinel> (inherited, not freshly injected) -> gh auth token prints only the sentinel ===
$ GH_TOKEN=ll-scoped-no-github-token gh auth token
ll-scoped-no-github-token
exit: 0

=== Re-check: ambient-only residual gap unchanged by the fix (no GH_TOKEN set at all) ===
$ env -u GH_TOKEN -u GITHUB_TOKEN GH_CONFIG_DIR=$EMPTY gh auth status
You are not logged into any GitHub hosts. To log in, run: gh auth login
exit: 1
$ env -u GH_TOKEN -u GITHUB_TOKEN GH_CONFIG_DIR=$EMPTY gh auth token | wc -c
exit: 0, len: 40 (value not recorded) -- still leaks the real Keychain token when no scope/sentinel is applied at all
