## claude --version
$ claude --version
stdout: '2.1.241 (Claude Code)\n'
stderr: ''
exit: 0

## claim 1: re-installing an already-installed plugin with -y exits 0 and reports 'already installed', not 'Successfully installed'
$ claude plugin install ll@little-loops -y
stdout: 'Installing plugin "ll@little-loops"...✔ Plugin "ll@little-loops" is already installed (scope: user)\n'
stderr: ''
exit: 0

## claim 2: without -y, in a non-TTY context (stdin=DEVNULL), install does not hang and exits non-zero
$ claude plugin install ll@little-loops
stdout: 'Installing plugin "ll@little-loops"...✔ Plugin "ll@little-loops" is already installed (scope: user)\n'
stderr: ''
exit: 0

## claim 3: installing a nonexistent plugin name exits non-zero with a clear error, no hang
$ claude plugin install nonexistent-plugin-xyz123@little-loops -y
stdout: 'Installing plugin "nonexistent-plugin-xyz123@little-loops"...\n'
stderr: '✘ Failed to install plugin "nonexistent-plugin-xyz123@little-loops": Plugin "nonexistent-plugin-xyz123" not found in marketplace "little-loops". Your local copy may be out of date — try `claude plugin marketplace update little-loops`.\n'
exit: 1

## claim 4: -y -s project run in a fresh scratch project dir exits 0 and writes the declaration into that dir's .claude/settings.json (not user config)
$ claude plugin install ll@little-loops -y -s project  (cwd=/var/folders/dg/j0h3vvbj20n6gd7077j8v5qr0000gn/T/ll-explore-api-proj-bw979m4z)
stdout: 'Installing plugin "ll@little-loops"...✔ Successfully installed plugin: ll@little-loops (scope: project)\n'
stderr: ''
exit: 0

project settings exists: True
project settings content:
{
  "enabledPlugins": {
    "ll@little-loops": true
  }
}

## claim 5: default scope (no -s) is 'user' -- inspect claim 1's stdout for '(scope: user)'
claim1 stdout contains '(scope: user)': True
